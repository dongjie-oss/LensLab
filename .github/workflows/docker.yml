name: 自动构建并推送Docker镜像到阿里云ACR

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths-ignore:
      - "README.md"
      - ".gitignore"
      - "*.png"
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（如 v1.1.0）'
        required: false
        type: string

env:
  ACR_REGISTRY: ${{ vars.ALIYUN_REGISTRY }}
  ACR_NAMESPACE: ${{ vars.ALIYUN_NAME_SPACE }}
  IMAGE_NAME: exposure-lab

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      BUILD_VERSION: ${{ github.event.inputs.version || 'dev-' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Aliyun ACR
        run: |
          echo "${{ secrets.ALIYUN_REGISTRY_PASSWORD }}" | \
          docker login ${{ env.ACR_REGISTRY }} \
            -u "${{ secrets.ALIYUN_REGISTRY_USER }}" \
            --password-stdin

      - name: Generate version tag
        id: tag
        run: |
          if [[ "${GITHUB_REF}" =~ ^refs/tags/v ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          else
            VERSION="latest"
          fi
          # 读取 backend/__version__.py 中的版本号
          PY_VERSION=$(grep -oP '"\K[0-9]+\.[0-9]+\.[0-9]+' backend/__version__.py 2>/dev/null || echo "1.0.1")
          echo "image_tag=${VERSION}" >> $GITHUB_OUTPUT
          echo "python_version=${PY_VERSION}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.image_tag }}
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.python_version }}
            ${{ env.ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_VERSION=${{ steps.tag.outputs.python_version }}
            BUILD_TIME=${{ github.event.head_commit.timestamp }}
